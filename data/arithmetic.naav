main {
    num numone = 20;
    num numtwo = 5;

    # Addition
    num sum;
    sum = numone + numtwo;
    print(sum);

    # Subtraction
    num difference;
    difference = numone - numtwo;
    print(difference);

    # Multiplication
    num product;
    product = numone * numtwo;
    print(product);

    # Division
    num quotient;
    quotient = numone / numtwo;
    print(quotient);
}